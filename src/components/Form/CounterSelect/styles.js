import { StyleSheet } from "react-native";
import { BaseColor } from "src/config";

export default StyleSheet.create({
  contentPicker: {
    padding: 10,
    borderRadius: 8,
    flex: 1,
    alignItems: "center",
  },
});
